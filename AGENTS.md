# AGENTS.md

Guidance for coding agents working in this repo lives in one canonical file:

**→ [`.agents/instructions.md`](.agents/instructions.md)**

Read it before doing anything. It covers what this repo is, the operating
envelope you are graded on (draft-PR-only, approved-repo-only, no secrets/
ungranted data/egress), how to run and deploy the `service/`, and the two-repo
split (workshop materials live in a separate repo). Edit guidance in
`.agents/instructions.md`, not here — this stub only points to it so `AGENTS.md`,
`CLAUDE.md`, and `GEMINI.md` never drift.
